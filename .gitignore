# The source extracts. The README carries the URLs they come from.
pt.jsonl
wiktextract-*.jsonl.gz

# Built page. `python3 pipeline/render_page.py` writes it from page_template.html and
# pt_levels.json, with no network and no binary behind it.
defining-pt.html

# The whole deploy directory is generated from pipeline/ by that same command, and the
# Vercel link inside it writes an OIDC token.
/site/
